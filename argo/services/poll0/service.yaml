apiVersion: v1
kind: Service
metadata:
  name: poll0
  labels:
    app.kubernetes.io/name: poll0
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: poll0
  ports:
    - name: http
      port: 80
      targetPort: http
